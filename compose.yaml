services:
  ieltstar:
    image: crpi-icp3lopw4m28wge3-vpc.cn-shanghai.personal.cr.aliyuncs.com/xmmtx/ieltstar:latest
    container_name: ieltstar
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      # MongoDB (required)
      DB_NAME: ieltstar
      DB_URL: mongodb://host.docker.internal:27017
      DB_USER: "ieltstar"
      DB_PASS: "zzajA5sXPj5CthRW"
      # Optional: SendGrid email
      # SENDGRID_APIKEY: your_key
      # FROM_EMAIL: noreply@example.com
      # Optional: Auth0 (set API_URL when using Auth0)
      # AUTH0_SECRET: your-secret
      # AUTH0_BASE_URL: https://ielts.xm233.cn
      # AUTH0_ISSUER_BASE_URL: https://your-tenant.auth0.com
      # AUTH0_CLIENT_ID: your-client-id
      # AUTH0_CLIENT_SECRET: your-client-secret
    extra_hosts:
      - "host.docker.internal:host-gateway"
